<div class="container">
  <div class="row no-gutters">
    <div class="col-md5 col-sm-10 mx-2 mx-sm-auto shadow-lg rounded px-0 my-5">
      <h5 class="text-dark bg-light p-4 mb-0">画像投稿フォーム</h5>
      <div class="form-body p-4">
        <% if @post_image.errors.any? %>
          <%= @post_image.errors.count %>件のエラーが発生しました
          <% post_image.errors.full_messages.each do |f| %>
            <%= f.attachment_field :image, class:"w-100" %>
            <div class="input-group mb-3 mt-4">
              <div class="input-group-prepend">
                <span class="input-group-text" aria-hidden="true"><i class="fas fa-building"></i></span>
              </div>
              <%= f.text_field :shop_name, class:"form-control", placeholder:"ショップ名" %>
            </div>
            <%= f.text_area :caption, class:"form-control", rows:"3", placeholder:"コメントをここに" %>
            <hr>
            <%= f.submit "投稿", class:"btn btn-lg btn-block btn-info mt-4" %>
          <% end %>
        </div>
    </div>
  </div>
</div>

<%
=begin
%>
  <% if @post_image.errors.any? %>
    <%= @post_image.errors.count %> 件のエラーが発生しました。
    <% @post_image.errors.full_messages.each do |message| %>
      <%= message %>
    <% end %>
  <% end %>

  <h1>画像投稿フォーム</h1>
  <%= form_with model: @post_image, local:true do |f| %>
    <h4>画像</h4>
    <%= f.attachment_field :image %>
    <h4>ショップ名</h4>
    <%= f.text_field :shop_name %>
    <h4>説明</h4>
    <%= f.text_area :caption %>
    <%= f.submit "投稿" %>
  <% end %>
<%
=end
%>